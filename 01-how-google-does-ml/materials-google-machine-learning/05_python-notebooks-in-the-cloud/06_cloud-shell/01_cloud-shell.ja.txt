ここではCompute Engine VMを作成しました 目的は単に幾つかのスクリプトに
名前を付けることでした 非常に無駄な操作です 実際には必要はありません 便利なものをお見せします GCPコンソールに行くと ここに小さいアイコンがあります 「Google Cloud Shellを起動してください」 これをクリックすると いわゆるCloud Shellが現れます Cloud ShellはマイクロVM つまり 微小なVMです でも これで十分です なぜなら今回の目的は単に ちょっと試して幾つかのスクリプトを実行し 幾つかのファイルを1つの場所から別の場所に コピーすることだけです これはCloud Shellです ここでは 先程Compute Engine VMで
行ったのと同じような 操作ができます ただし Cloud Shell は開発者用である
という点が違います それでCompute Engine VMに
インストールした多くの機能を ここではインストール不要です 例えばGitは既に存在します それでGitが必要であれば 単にGitクローンするだけです こうしてリポジトリをクローンすれば完了です これで必要なコードがすべて揃いました そしてCompute Engineで行った
ほとんどのラボを実行できます Cloud Shellで実行できるのです Cloud Shellはとても一過性の高いVMです 使用されない場合 大抵は一時間以内にリサイクルされます でも いつでも別のCloud Shellを開始できます Cloud Shellで別のタブを作れます 依然として同じVMですが その中に2つのタブができます このタブで別のコマンドを実行できます それでCloud Shellという手段で
例えばデータラボを開始できます 次のラボでは これをやってみます